#include <iostream>

using namespace std;

int main() {
    // Note: The angular brackets are opposite
    double x;
    double y;

    cout << "Enter the values for x: ";
    cin >> x;

    cout << "Enter the values for y5: ";
    cin >> y;
    
    cout << "Sum: " << x + y;
    
    return 0;
}
